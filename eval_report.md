# AI Infra LoRA SFT 训练评测报告

> 生成时间：2026-06-03 03:03:19
> 基座模型：Qwen/Qwen3-0.6B
> LoRA 输出：./lora_sft_ai_infra_audio_video_output
> 评估样本数：50

---

## 1. Perplexity (PPL) 对比

PPL 衡量模型对文本的"惊讶程度"，越低越好。
但 PPL 低 ≠ 生成质量高，需结合 ROUGE 和人工评估。

| 模型 | Avg Loss | Avg PPL |
|------|----------|---------|
| Base | 3.8353 | 46.31 |
| LoRA | 2.9230 | 18.60 |


> **解读**: 若 LoRA 模型 PPL 远低于 Base，说明模型已学到数据分布特征。
> 若 LoRA 模型 PPL 接近于 1，可能是过拟合信号。

---

## 2. ROUGE-L F1 对比

ROUGE-L 基于最长公共子序列（LCS），衡量生成答案与参考答案的字符级重叠度。
F1 越高，生成结果越接近参考答案。

| 模型 | ROUGE-L F1 | 样本数 |
|------|-----------|--------|
| Base | 0.1087 | 50 |
| LoRA | 0.1356 | 50 |


> **解读**: ROUGE-L 对开放式长回答不太敏感（答案可以不同但都正确）。
> F1 > 0.3 通常表示内容有实质重叠。配合人工抽查判断质量。

---

## 3. 生成样本对比

以下对比 Base 模型与 LoRA 模型对相同问题的回答（前 5 条）。

### 样本 1

**问题**: 简要介绍Swin Transformer的窗口注意力和移位窗口机制，为什么它能作为通用视觉骨干替代CNN？

| 角色 | 内容 |
|------|------|
| 📝 参考答案 | Swin Transformer在非重叠窗口内计算自注意力，计算量由O(N²)降为O(N*W²)（W为窗口大小）；通过Layer层的移位操作，使不同窗口的信息可以混合。这种层次化结构和线性复杂度使其能够生成多尺度特征图，直接替代ResNet等CNN骨干。在我的目标检测微调中，Swin-Tiny精度超过ResNet50。 |
| 🔴 Base 模型 | system
You are a helpful assistant.
user
简要介绍Swin Transformer的窗口注意力和移位窗口机制，为什么它能作为通用视觉骨干替代CNN？
assistant
Swin Transformer是一种基于Transformer架构的多模态预训练模型，可以用于图像、文本等多模态任务。它的窗口注意力机制通过在每个时间步内对输入进行局部窗口处理，使得模型能够更好地捕捉到上下文信息。

移位窗口机制则是将时间步与位置步结合起来，通过对当前时间和位置的窗口处理来提取特征。这种机制使得模型能够在不同的时间步上学习到不同的特征，并且可以在不同位置上提取到相同的特征。

Swin Transformer之所以能作为通用视觉骨干替代CNN，是因为其窗口注意力机制和移位窗口机制都具有强大的自适应性和泛化能力。这些机制使得模型能够在不同的任务中表现得更好，同时也能有效地利用更多的数据资源。此外，Swin Transformer还采用了更先进的网络结构和优化方法，进一步提高了模型的性能和效率。 |
| 🟢 LoRA 模型 | system
You are a helpful assistant.
user
简要介绍Swin Transformer的窗口注意力和移位窗口机制，为什么它能作为通用视觉骨干替代CNN？
assistant
### 面试官目的
考察对Transformer在图像处理中的应用深度理解。

### 回答思路
解释窗口注意力如何通过滑动窗口计算局部特征图，并与位置信息结合。说明移位窗口如何将不同尺度的注意力重叠在一起，形成更丰富的特征表示。

### 思考过程
Swin Transformer使用窗口注意力，每个窗口包含来自不同分辨率的特征向量。通过滑动窗口（如3x3或5x5），可以捕获到不同大小的区域特征。同时，由于其多尺度结构，能够融合来自不同分辨率的全局信息，增强图像识别能力。

窗口注意力将输入映射为一个窗口内的所有像素值的线性组合，然后通过加权平均得到最终输出。移位窗口则将该窗口内的所有窗口值相加，进一步增强局部细节。Swin Transformer利用这些机制，不仅在视频编码中表现优异，而且在图像分类、分割等任务中可获得显著提升。 |

---

### 样本 2

**问题**: 解释C++的RAII机制，并举例说明如何在你的项目中应用。

| 角色 | 内容 |
|------|------|
| 📝 参考答案 | RAII将资源分配与对象构造绑定，释放与析构绑定。例如自定义`std::lock_guard`类似的互斥锁守卫；或者`cudaEvent_t`包装类，构造时cudaEventCreate，析构时cudaEventDestroy。这样即使抛出异常也能正确释放资源，代码更安全。 |
| 🔴 Base 模型 | system
You are a helpful assistant.
user
解释C++的RAII机制，并举例说明如何在你的项目中应用。
assistant
RAII（Resource Acquisition Is Initialization）是一种面向对象编程中的概念，它允许你在创建一个资源时自动获取和释放该资源。这使得代码更加简洁、易于维护。

### RAII 的基本思想

1. **资源获取**：当你需要使用某个资源时，首先调用 `std::unique_ptr` 或 `std::shared_ptr` 的构造函数来获取这个资源。
2. **资源释放**：当不再需要这个资源时，调用析构函数将其释放。

### RAII 的应用场景

#### 1. 错误处理

在程序中，你可能需要捕获异常并进行错误处理。例如，在网络请求中，你可以尝试连接到服务器，如果失败则抛出异常。

```cpp
#include <iostream>
#include <string>

void connectToServer() {
    std::string url = "http://e... |
| 🟢 LoRA 模型 | system
You are a helpful assistant.
user
解释C++的RAII机制，并举例说明如何在你的项目中应用。
assistant
### 面试官目的
考察对C++ RAII底层实现的理解，以及在实际开发中的应用。

### 回答思路
先介绍RAII的基本概念（类、对象、指针），然后给出一个简单的例子，如创建文件句柄时使用`std::shared_ptr<FILE>`，并讨论其优势和局限性。

### 思考过程
RAII允许你将资源管理封装到类中，避免手动释放内存。例如，文件句柄可以被初始化为`std::shared_ptr<File>`，当不再需要时自动释放。

我用C++17引入了`std::unique_ptr<>`来替代`new std::shared_ptr<>`，并在构造函数中调用`std::unique_lock<std::mutex> lock;`锁定锁，释放时使用`lock.unlock();`。这样代码更简洁，且提高了性能。我的项目中，文件句柄由`std::shared_ptr<io::File>`管理，每次打开或关闭都通过`open_... |

---

### 样本 3

**问题**: 请描述Whisper-large-v3结合VAD实现流式语音识别的设计，以及如何控制单段延迟小于1秒。

| 角色 | 内容 |
|------|------|
| 📝 参考答案 | 系统采用WebRTC VAD分析每帧，检测到语音激活后将PCM数据写入环形缓冲区，当连续静音超过500ms或累计语音达到2s时，将缓冲区数据截取为一个语音段。Whisper-large-v3运行在4090上，2s音频推理约150ms。通过流水线，当VAD判断语音段结束时立即提交推理任务，从语音结束到识别文本输出平均延迟800ms。同时，对于长句，采用流式分片尾包发送，进一步减小首字延迟。 |
| 🔴 Base 模型 | system
You are a helpful assistant.
user
请描述Whisper-large-v3结合VAD实现流式语音识别的设计，以及如何控制单段延迟小于1秒。
assistant
Whisper-large-v3是一个基于深度学习的语音识别模型，它使用了Transformer架构和卷积神经网络（CNN）来处理音频数据。在Whisper-large-v3中，我们使用了一个叫做VAD（Voice Activity Detection）的模块来检测声音中的活动区域。

VAD的主要功能是检测声音中的活动区域，例如说话者的位置、说话者的动作等。通过检测这些活动区域，我们可以确定说话者正在说话的时间，并据此进行语音识别。

为了实现流式语音识别，我们需要将语音信号分割成多个片段，然后对每个片段进行语音识别。在这个过程中，我们需要控制每个片段的延迟，以确保它们之间的同步性。

对于单段延迟小于1秒的情况，我们可以采取以下措施：

1. 使用一个低延迟的传输协议：例如UDP或TCP，因为它们通常具有较低的延迟。

2. 在发送端添加一些额外的数据包，用于确认接收方已经接收到... |
| 🟢 LoRA 模型 | system
You are a helpful assistant.
user
请描述Whisper-large-v3结合VAD实现流式语音识别的设计，以及如何控制单段延迟小于1秒。
assistant
### 面试官目的
考察对大规模模型在实时应用中的能力。

### 回答思路
说明使用多线程录音、多路VAD（如TTS），并给出延时监控和重传机制。

### 思考过程
我们采用两组录音线程：一组记录原始音频，另一组预测VAD结果；中间通过队列同步。每40ms检查当前线程的输出状态，若未发送或错误则重试。如果目标是1s内完成识别，需保证每个线程都至少发送一次。

实现在两个线程中，一个记录器负责采样，另一个预测器用预训练的VAD模型预测下一帧。通过`cv2.waitKey(40)`定时器检测是否发出了新的样本。同时设置重试策略，例如每500ms尝试一次，失败则丢弃。这样即使单个线程超时，也保证了总延迟不超过1s。 |

---

### 样本 4

**问题**: 如何利用知识蒸馏压缩CNN模型用于实时视频处理？请描述训练流程。

| 角色 | 内容 |
|------|------|
| 📝 参考答案 | 训练学生时，温度T升高平滑分布，总loss = α * KL(softmax_teacher/T, softmax_student/T) + (1-α) * CE(y, student)。在视频行为分类中，我用此方法将模型缩小4倍，精度仅降2%，达到实时。 |
| 🔴 Base 模型 | system
You are a helpful assistant.
user
如何利用知识蒸馏压缩CNN模型用于实时视频处理？请描述训练流程。
assistant
1. 数据预处理：首先，我们需要对输入的视频数据进行预处理，包括图像增强、去噪、裁剪等操作。

2. 特征提取：然后，我们使用卷积神经网络（CNN）来提取特征。在这个过程中，我们可以将原始图像转换为特征向量，这些特征可以是像素值、纹理信息、形状和结构信息等。

3. 知识蒸馏：接下来，我们将CNN模型中的参数传递给另一个CNN模型，这个过程称为知识蒸馏。通过这种方式，我们可以从一个模型中学习到一些有用的特征，然后将其应用到另一个模型上，以提高其性能。

4. 实时视频处理：最后，我们将蒸馏后的CNN模型应用于实时视频处理任务。例如，我们可以使用它来检测目标物体、识别面部表情、预测下一个动作等。

5. 模型优化：在实际应用中，我们可能需要不断调整CNN模型的参数，以适应新的数据集或不同的应用场景。

6. 评估与验证：在完成所有步骤后，我们需要对模型进行评估和验证，以确保其性能达到预期水平。

7. 部署：一旦模型经过充... |
| 🟢 LoRA 模型 | system
You are a helpful assistant.
user
如何利用知识蒸馏压缩CNN模型用于实时视频处理？请描述训练流程。
assistant
### 面试官目的
考察对深度学习在边缘计算中的应用理解，以及实际工程化能力。

### 回答思路
先介绍知识蒸馏原理，然后说明训练过程：将预训练的CNN权重作为输入，通过监督学习生成新的权重；最后用新权重替换原网络。

### 思考过程
知识蒸馏是通过微调一个预训练的CNN来生成一个新的CNN。我们使用了ImageNet 100K的预训练权重，通过目标检测任务验证新权重的有效性。然后将新权重作为输入，与原始CNN进行对比，调整参数以提高性能。

我首先加载预训练的ResNet-50和ImageNet-100k的权重，创建两个模型：`ResNet_50_Prototate`和`New_ResNet`. `ResNet_50_Prototate`为输入图像生成权重，通过损失函数（如Focal Loss）优化。`New_ResNet`保留原始权重，直接输入到目标检测任务中。训练过程中，`New_ResNet`不断更新其权重，... |

---

### 样本 5

**问题**: 什么是C++的CRTP（奇异递归模板模式）？有什么应用？

| 角色 | 内容 |
|------|------|
| 📝 参考答案 | `template<typename T> class Base { void interface() { static_cast<T*>(this)->impl(); } };`。相比虚函数，无运行时开销。适用于性能关键且类型确定的场景。我的CUDA资源管理类中使用了类似技巧。 |
| 🔴 Base 模型 | system
You are a helpful assistant.
user
什么是C++的CRTP（奇异递归模板模式）？有什么应用？
assistant
C++的CRTP（奇异递归模板模式）是一种设计模式，它允许一个类继承另一个类，并且这个继承关系可以是递归的。这种模式使得代码更加简洁和易于维护。

在C++中，CRTP通常用于实现多态性。当一个类需要处理多个不同的数据类型时，可以通过CRTP来实现多态性。例如，假设我们有一个抽象基类`Base`，然后子类`Derived`继承自`Base`。那么，当我们调用`Derived::method()`时，编译器会自动将`Derived`作为参数传递给`Base`的构造函数，从而实现多态性。

下面是一个简单的例子：

```cpp
class Base {
public:
    virtual void method() = 0;
};

class Derived : public Base {
public:
    void method() override {
        std::cout << "Method ca... |
| 🟢 LoRA 模型 | system
You are a helpful assistant.
user
什么是C++的CRTP（奇异递归模板模式）？有什么应用？
assistant
### 面试官目的
算法抽象。

### 回答思路
C++17引入，用于实现多态。通过模板参数`typename T::template Function`，将函数封装为类。

### 思考过程
例如`std::vector<T>::push_back`可以被包装成`std::vector<std::vector<T>>`，使用C++14的泛型。

`template<typename T> class Vector;`，`Vector<T>::push_back`成为`Vector<Vector<T>>`。这样代码更清晰，也实现了多态。 |

---



## 4. 结论与建议

| 指标 | 判断标准 | 是否合格 |
|------|---------|---------|
| PPL 下降 | LoRA PPL < Base PPL | 待填充 |
| ROUGE-L > 0.2 | 生成与参考答案有实质重叠 | 待填充 |
| 过拟合检测 | PPL 未接近 1.0 | 待填充 |

> **下一步建议**:
> 1. 若 ROUGE-L 过低而 PPL 正常 → 模型学到了风格但未学到内容 → 增加 epoch 或降低 lr
> 2. 若 PPL 接近 1.0 → 严重过拟合 → 降低 epoch、增大 dropout、减少 LoRA rank
> 3. 若 PPL 不降 → 学习率过低或数据格式有误 → 检查 chat_template
> 4. 推荐引入 LLM-as-Judge（GPT-4/Claude）做多维度打分，替代纯 ROUGE

---

*报告由 eval.py 自动生成*

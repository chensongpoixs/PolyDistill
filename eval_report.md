# AI Infra LoRA SFT 训练评测报告

> 生成时间：2026-06-03 11:53:35
> 基座模型：Qwen/Qwen3-0.6B
> LoRA 输出：./lora_sft_ai_infra_audio_video_output
> 评估样本数：50

---

## 1. Perplexity (PPL) 对比

PPL 衡量模型对文本的"惊讶程度"，越低越好。
但 PPL 低 ≠ 生成质量高，需结合 ROUGE 和人工评估。

| 模型 | Avg Loss | Avg PPL |
|------|----------|---------|
| Base | 3.9521 | 52.04 |
| LoRA | 1.6767 | 5.35 |


> **解读**: 若 LoRA 模型 PPL 远低于 Base，说明模型已学到数据分布特征。
> 若 LoRA 模型 PPL 接近于 1，可能是过拟合信号。

---

## 2. ROUGE-L F1 对比

ROUGE-L 基于最长公共子序列（LCS），衡量生成答案与参考答案的字符级重叠度。
F1 越高，生成结果越接近参考答案。

| 模型 | ROUGE-L F1 | 样本数 |
|------|-----------|--------|
| Base | 0.0789 | 50 |
| LoRA | 0.1446 | 50 |


> **解读**: ROUGE-L 对开放式长回答不太敏感（答案可以不同但都正确）。
> F1 > 0.3 通常表示内容有实质重叠。配合人工抽查判断质量。

---

## 3. 生成样本对比

以下对比 Base 模型与 LoRA 模型对相同问题的回答（前 5 条）。

### 样本 1

**问题**: 如何用CNN实现视频的实时美颜和滤镜？需要什么样的网络结构？

| 角色 | 内容 |
|------|------|
| 📝 参考答案 | 移动端视频美颜CNN采用轻量U-Net结构，输入为单帧和人脸分割掩码，输出为美化后的帧。通过GAN损失让皮肤变光滑，感知损失保持五官清晰。使用MNN或NCNN部署，结合GPU Shader进行磨皮融合，可在手机上达到30fps以上。 |
| 🔴 Base 模型 | system
You are a helpful assistant.
user
如何用CNN实现视频的实时美颜和滤镜？需要什么样的网络结构？
assistant
<think>
好的，用户问的是如何用CNN实现视频的实时美颜和滤镜，需要什么样的网络结构。首先，我得先理解用户的需求。美颜和滤镜通常需要处理视频中的面部特征，比如肤色、眼睛、鼻子等，同时还要保持视频的流畅和实时性。用户可能希望得到一个结构清晰、易于理解的CNN模型，可能用于移动端或者嵌入式设备，所以网络结构需要考虑效率和实时性。

接下来，我需要回忆一下常见的视频处理模型。比如，YOLO系列、EfficientNet、ResNet等，但这些通常用于目标检测，而美颜和滤镜可能需要更专注于图像处理。可能需要结合目标检测和图像增强，或者使用更简单的结构，比如卷积神经网络。

然后，用户提到实时性，所以网络结构需要尽可能小，参数少，计算量小。可能需要使用轻量级的模型，比如MobileNet或EfficientNet，或者使用更高效的架构，比如MobileNetV2。同时，实时处理视频可能需要使用流式处理，比如TensorRT或者P... |
| 🟢 LoRA 模型 | system
You are a helpful assistant.
user
如何用CNN实现视频的实时美颜和滤镜？需要什么样的网络结构？
assistant
<think>

</think>

### 面试官目的
视频生成与编辑中的CNN应用。
### 回答思路
使用U-Net或残差网络作为基础，然后添加GAN或GAN-Discrete作为生成器，同时使用另一个CNN作为鉴别器，或使用Pointwise卷积处理多通道。
### 思考过程
实时美颜需能快速、高效地学习人脸归一化后的纹理。通常做法是将输入特征图除以均值方差得到0-1区间，送入U-Net。也可采用GAN生成器：将输入MPC特征与一个字典（由FLGAN生成的判别器）相乘，生成器输出0-1掩码，反向传播优化生成器和判别器。为加速，可使用Pointwise卷积代替2D卷积。

实时视频美颜CNN结构通常包含：1) 转换层（如ReLU后送入CNN）；2) U-Net或残差网络提取人脸归一化后的特征；3) 生成器（如GAN）将输入MPC特征与预训练的判别器特征相乘，通过Pointwise卷积一次卷积到0-1空间，送入U-Net... |

---

### 样本 2

**问题**: 解释一下SmoothQuant量化的核心思想，与AWQ的主要区别是什么？

| 角色 | 内容 |
|------|------|
| 📝 参考答案 | SmoothQuant：W8A8，适用于需要极致加速（如INT8 gemm）的场景，但需要校准激活异常值。AWQ：W4A16，更注重权重压缩而不量化激活，在vLLM中应用广泛。我们选择AWQ因为vLLM对W4A16支持成熟，精度保持更好。 |
| 🔴 Base 模型 | system
You are a helpful assistant.
user
解释一下SmoothQuant量化的核心思想，与AWQ的主要区别是什么？
assistant
<think>
嗯，用户问的是SmoothQuant量化的核心思想和与AWQ的区别。首先，我需要回忆一下这两个概念的定义。SmoothQuant应该是指一种基于梯度的量化方法，而AWQ可能是指另一种量化方法，比如基于梯度的量化或者某种特定的量化策略。

首先，核心思想方面，SmoothQuant可能是在量化过程中利用梯度的平滑性，避免梯度爆炸，从而减少计算量。而AWQ可能有不同的实现方式，比如使用不同的量化策略，或者处理梯度的方式不同。需要确认这两个方法的具体实现细节，比如是否使用了梯度的平滑处理，或者是否引入了其他机制。

然后，区别方面，可能包括量化范围的选择、计算复杂度、适用场景等。比如，SmoothQuant可能更适用于需要高精度的场景，而AWQ可能在某些特定任务中表现更好。另外，是否涉及梯度的平滑处理，或者是否使用了不同的量化策略，比如基于参数的量化或基于参数的梯度压缩。

用户可能是在研究量化技术，或... |
| 🟢 LoRA 模型 | system
You are a helpful assistant.
user
解释一下SmoothQuant量化的核心思想，与AWQ的主要区别是什么？
assistant
<think>

</think>

### 面试官目的
深入量化范式理解。

### 回答思路
SmoothQuant通过统计拟合找到量化参数，AWQ是更直接的量化方案。SmoothQuant对激活和权重使用量化器，保留精度通过平滑分布。

### 思考过程
AWQ对矩阵进行逐元素量化，对权重和激活分别量化，最后缩放。SmoothQuant使用量化器处理激活和权重，输出平滑的量化值作为整数缩放因子，牺牲部分计算精度换取更稳定的统计量。

SmoothQuant基于概率密度估计量化参数，对权重和激活使用量化器，输出平滑的数值，然后通过缩放恢复原分布。这比AWQ更鲁棒，但引入smoothness constraint，稍受限制。 |

---

### 样本 3

**问题**: 你提到的“自研P2P中转服务器”和标准TURN服务器有什么区别？为什么选择自研？

| 角色 | 内容 |
|------|------|
| 📝 参考答案 | 自研中转服务完全去掉了STUN协商和权限机制，会话建立由信令服务控制，中继服务仅需根据信令指令建立一对一通道，监听端口接收客户端TCP连接或UDP包，并互相转发。由于不引入TURN协议，减少了编解码开销，延迟降低约5ms。同时可以根据业务需要灵活插入限流、监控等功能。缺点是缺乏标准化，无法被第三方WebRTC客户端直接使用。在我们的闭环生态中，这是最优选择。 |
| 🔴 Base 模型 | system
You are a helpful assistant.
user
你提到的“自研P2P中转服务器”和标准TURN服务器有什么区别？为什么选择自研？
assistant
<think>
嗯，用户问的是“自研P2P中转服务器”和“标准TURN服务器”的区别，以及为什么选择自研。首先，我需要确认这两个概念的具体定义，确保自己理解正确。P2P中转服务器通常指的是在分布式网络中，将数据从源节点传输到目标节点的中间节点，而TURN服务器则是用于网络视频会议中的中转服务器，负责在会议中转过程中进行数据的转发和处理。

接下来，用户可能想知道它们在功能、性能、成本等方面的不同。比如，自研服务器可能具备更高的灵活性和定制化能力，而标准服务器可能在某些方面有更成熟的技术支持。但用户可能没有明确提到这些点，所以需要详细展开。

然后，为什么选择自研呢？可能的原因包括更高的定制化需求，比如需要支持特定的协议或功能；更灵活的扩展能力，因为自研可以更容易地集成新的功能；或者对性能和安全性有更高的要求，自研服务器可能在这些方面有更好的控制和优化。此外，自研服务器可能更符合企业的特定需求，比如成本控制... |
| 🟢 LoRA 模型 | system
You are a helpful assistant.
user
你提到的“自研P2P中转服务器”和标准TURN服务器有什么区别？为什么选择自研？
assistant
<think>

</think>

### 面试官目的
评估架构选型能力，区分开源与自研。

### 回答思路
标准TURN使用NATNAT转换，自研无需配置，性能可定制，支持QoS。

### 思考过程
TURN依赖NAT，我们自建中转，无需处理外部NAT，且性能可满足业务需求。同时支持灵活的QoS。

自研P2P中转服务器是完全独立的，无需TURN服务器的依赖，性能可调，支持动态路由和QoS标记，且不遵循标准。我为中转服务设计了专门的端口和连接管理，实现了低延迟和高吞吐。 |

---

### 样本 4

**问题**: 什么是大模型中的提示压缩（Prompt Compression）中的LLMLingua？如何利用小模型压缩提示？

| 角色 | 内容 |
|------|------|
| 📝 参考答案 | LLMLingua利用轻量语言模型计算长提示中各token的困惑度，保留高困惑度（信息量大）的token，丢弃冗余token，实现可控比例的提示压缩。压缩后的短提示送入大模型，在几乎不损失性能的情况下大幅节省推理成本。 |
| 🔴 Base 模型 | system
You are a helpful assistant.
user
什么是大模型中的提示压缩（Prompt Compression）中的LLMLingua？如何利用小模型压缩提示？
assistant
<think>
好的，用户问的是大模型中的提示压缩（Prompt Compression）中的LLM中的“LLM”具体指的是什么，以及如何利用小模型压缩提示。首先，我需要确认用户的问题是否正确，可能用户对LLM和提示压缩的术语有混淆。

接下来，我应该先解释LLM的全称，即Large Language Model，也就是大语言模型。然后，用户提到的“LLM中的LLM”可能是指LLM本身，但需要明确LLM是大语言模型，而提示压缩是压缩提示信息的方法。可能用户想问的是如何将大模型的提示信息压缩到更小的模型中，或者LLM如何处理提示压缩的问题。

然后，我需要考虑用户可能的背景。他们可能对大模型和提示压缩不太熟悉，或者遇到了实际应用中的问题，比如如何在不损失模型性能的情况下减少提示长度。这时候需要详细解释提示压缩的基本概念，以及如何通过压缩提示来优化模型性能。

另外，用户可能想... |
| 🟢 LoRA 模型 | system
You are a helpful assistant.
user
什么是大模型中的提示压缩（Prompt Compression）中的LLMLingua？如何利用小模型压缩提示？
assistant
<think>

</think>

### 面试官目的
了解提示工程与小模型交互。
### 回答思路
LLMLingua使用一个小型语言模型（如GPT-2）对用户输入的提示进行编码，快速生成压缩后的摘要或掩码，大幅降低大模型的提示长度。
### 思考过程
大模型处理长提示时计算量巨大。LLMLingua将长提示转化为若干查询，由一个小模型回答，将原提示编码为k*v对的掩码或摘要。利用小模型的高效推理，快速得到提示的浓缩表示，然后传递给大模型，减少其计算和内存读写。

LLMLingua利用一个小语言模型对长提示进行去重和压缩，将复杂的文本转化为简洁的摘要或MQ。这个浓缩的摘要作为输入到大模型，极大地减少了大模型处理长提示时的开销和数据量。 |

---

### 样本 5

**问题**: 在行人重识别中，如何解决跨视角的“外观变异”问题？多尺度全局-局部特征融合网络是如何设计的？

| 角色 | 内容 |
|------|------|
| 📝 参考答案 | 行人重识别网络（如PCB）将特征图水平切分，每块做独立分类损失，使得模型学到视角变化下稳定的局部细节（如背包、鞋子），显著提升跨摄像头匹配精度。 |
| 🔴 Base 模型 | system
You are a helpful assistant.
user
在行人重识别中，如何解决跨视角的“外观变异”问题？多尺度全局-局部特征融合网络是如何设计的？
assistant
<think>
好的，用户问的是在行人重识别中如何解决跨视角的“外观变异”问题，以及多尺度全局-局部特征融合网络的设计。我需要先理解这两个问题的具体内容。

首先，跨视角的外观变异，可能指的是在不同视角下，比如从不同角度拍摄的行人，他们的面部特征可能有变化，导致识别困难。这时候需要解决这些变异，可能需要使用一些特征增强的方法，比如多尺度特征融合或者对抗训练。

然后是多尺度全局-局部特征融合网络的设计。这里可能需要考虑如何将全局的特征和局部的特征结合起来，以提高识别的准确性。可能需要使用卷积神经网络，比如U-Net，或者结合注意力机制，让网络在不同尺度上学习特征，从而增强特征的多样性。

接下来，我需要回忆相关的研究。比如，多尺度特征融合通常使用不同的卷积层，比如上采样层和下采样层，或者使用多尺度的特征提取器。全局和局部的结合可能涉及到将全局的特征和局部的特征进行拼接，或者通过注意力机制来整合... |
| 🟢 LoRA 模型 | system
You are a helpful assistant.
user
在行人重识别中，如何解决跨视角的“外观变异”问题？多尺度全局-局部特征融合网络是如何设计的？
assistant
<think>

</think>

### 面试官目的
跨视角鲁棒。
### 回答思路
全局特征 + 局部特征拼接或权值分配。
### 思考过程
不同相机角度下，面孔可能缩放、旋转、抖动。特征融合网络（如ResNet+ResNet101）在输入上同时传入全局（如MTCNN）和局部（如单帧MTCNN）特征，让系统自适应处理不同尺度和角度。

融合网络将MTCNN提取的全局特征和局部特征拼接，或在全局特征上加权，让模型能利用全景视图中的全局结构（如人体轮廓）和局部精细特征（如眼睛、鼻子）进行跨视角的身份匹配。 |

---



## 4. 结论与建议

| 指标 | 判断标准 | 是否合格 |
|------|---------|---------|
| PPL 下降 | LoRA PPL < Base PPL | 待填充 |
| ROUGE-L > 0.2 | 生成与参考答案有实质重叠 | 待填充 |
| 过拟合检测 | PPL 未接近 1.0 | 待填充 |

> **下一步建议**:
> 1. 若 ROUGE-L 过低而 PPL 正常 → 模型学到了风格但未学到内容 → 增加 epoch 或降低 lr
> 2. 若 PPL 接近 1.0 → 严重过拟合 → 降低 epoch、增大 dropout、减少 LoRA rank
> 3. 若 PPL 不降 → 学习率过低或数据格式有误 → 检查 chat_template
> 4. 推荐引入 LLM-as-Judge（GPT-4/Claude）做多维度打分，替代纯 ROUGE

---

*报告由 eval.py 自动生成*

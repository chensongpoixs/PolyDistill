# AI Infra LoRA SFT 训练评测报告

> 生成时间：2026-06-03 18:31:18
> 基座模型：Qwen/Qwen3-0.6B
> LoRA 输出：./lora_sft_ai_infra_audio_video_output
> 评估样本数：50

---

## 1. Perplexity (PPL) 对比

PPL 衡量模型对文本的"惊讶程度"，越低越好。
但 PPL 低 ≠ 生成质量高，需结合 ROUGE 和人工评估。

| 模型 | Avg Loss | Avg PPL |
|------|----------|---------|
| Base | 3.9067 | 49.73 |
| LoRA | 2.9042 | 18.25 |


> **解读**: 若 LoRA 模型 PPL 远低于 Base，说明模型已学到数据分布特征。
> 若 LoRA 模型 PPL 接近于 1，可能是过拟合信号。

---

## 2. ROUGE-L F1 对比

ROUGE-L 基于最长公共子序列（LCS），衡量生成答案与参考答案的字符级重叠度。
F1 越高，生成结果越接近参考答案。

| 模型 | ROUGE-L F1 | 样本数 |
|------|-----------|--------|
| Base | 0.0779 | 50 |
| LoRA | 0.1453 | 50 |


> **解读**: ROUGE-L 对开放式长回答不太敏感（答案可以不同但都正确）。
> F1 > 0.3 通常表示内容有实质重叠。配合人工抽查判断质量。

---

## 3. 生成样本对比

以下对比 Base 模型与 LoRA 模型对相同问题的回答（前 5 条）。

### 样本 1

**问题**: 如何用CNN实现视频的实时美颜和滤镜？需要什么样的网络结构？

| 角色 | 内容 |
|------|------|
| 📝 参考答案 | 移动端视频美颜CNN采用轻量U-Net结构，输入为单帧和人脸分割掩码，输出为美化后的帧。通过GAN损失让皮肤变光滑，感知损失保持五官清晰。使用MNN或NCNN部署，结合GPU Shader进行磨皮融合，可在手机上达到30fps以上。 |
| 🔴 Base 模型 | system
You are a helpful assistant.
user
如何用CNN实现视频的实时美颜和滤镜？需要什么样的网络结构？
assistant
<think>
嗯，用户问的是如何用CNN实现视频的实时美颜和滤镜，需要什么样的网络结构。首先，我得理解用户的需求。他们可能是在开发视频处理应用，想用深度学习来美化视频内容，比如滤镜效果。用户可能对CNN的结构不太熟悉，所以需要详细解释。

首先，我应该考虑视频处理通常需要哪些步骤。实时处理意味着模型需要高效，不能有延迟。所以网络结构需要尽可能小，参数少，计算量小。然后，美颜和滤镜可能需要处理视频帧，所以可能需要将视频分割成多个帧，或者使用视频编码器来处理。

接下来，美颜和滤镜通常涉及图像处理，但视频是动态的，所以可能需要将视频帧进行预处理，比如降采样或增强，然后应用滤镜。或者，可能需要将视频帧作为输入，通过CNN来生成滤镜效果。

然后，网络结构方面，可能需要使用卷积层来提取特征，比如使用ResNet、VGG等经典模型，或者更简单的结构。但考虑到实时性，可能需要更高效的结构，比如MobileNet或EfficientNe... |
| 🟢 LoRA 模型 | system
You are a helpful assistant.
user
如何用CNN实现视频的实时美颜和滤镜？需要什么样的网络结构？
assistant
<think>
### 面试官目的
视频处理的轻量级CNN。
### 回答思路
使用轻量CNN（如ResNet或MobileNet）作为基础，加上轻量级的滤镜和美颜模块。
### 思考过程
美颜通常需要对人脸进行锐化、对比度增强、去噪等。可将这些操作分解为多个轻量CNN模块，如锐化网络、对比度增强网络、去噪网络等，通过轻量级卷积和归一化实现。
</think>

轻量CNN（如ResNet-18或MobileNetV2）作为基础网络，然后在每个滤镜模块上添加轻量卷积和归一化，通过轻量级的滤镜网络实现人脸的美颜和滤镜效果。 |

---

### 样本 2

**问题**: 解释一下SmoothQuant量化的核心思想，与AWQ的主要区别是什么？

| 角色 | 内容 |
|------|------|
| 📝 参考答案 | SmoothQuant：W8A8，适用于需要极致加速（如INT8 gemm）的场景，但需要校准激活异常值。AWQ：W4A16，更注重权重压缩而不量化激活，在vLLM中应用广泛。我们选择AWQ因为vLLM对W4A16支持成熟，精度保持更好。 |
| 🔴 Base 模型 | system
You are a helpful assistant.
user
解释一下SmoothQuant量化的核心思想，与AWQ的主要区别是什么？
assistant
<think>
嗯，用户问的是SmoothQuant量化的核心思想和与AWQ的区别。首先，我需要回忆一下这两个概念的定义。SmoothQuant应该是指一种基于梯度的量化方法，而AWQ可能是指另一种量化方法，比如基于梯度的量化或者某种特定的量化策略。

首先，SmoothQuant的核心思想可能涉及如何在保持模型精度的同时，减少计算量。比如，通过在量化过程中使用梯度下降，或者通过某种方式将高维特征映射到低维空间，从而减少计算量。而AWQ可能有不同的实现方式，比如使用不同的量化策略，或者不同的优化方法。

接下来，我需要确认这两个方法的具体区别。比如，SmoothQuant可能更注重在量化过程中保持模型的可解释性，而AWQ可能更侧重于减少计算量。或者，可能SmoothQuant使用的是梯度的平滑处理，而AWQ使用的是其他类型的处理方式。

另外，用户可能对这两个方法的应用场景和优缺点不太清楚，所以需要详细解释它们的... |
| 🟢 LoRA 模型 | system
You are a helpful assistant.
user
解释一下SmoothQuant量化的核心思想，与AWQ的主要区别是什么？
assistant
<think>
### 面试官目的
量化原理。

### 回答思路
SmoothQuant在量化前对整数数据进行线性插值，保持精度；AWQ使用可学习的线性映射，量化后精度下降。

### 思考过程
AWQ量化后精度下降，但可学习的线性映射能补偿。SmoothQuant量化前对整数进行线性插值，保持精度，但量化后精度下降。AWQ量化后精度下降，但可学习的线性映射能补偿。
</think>

AWQ量化后精度下降，但通过可学习的线性映射，可以补偿量化误差。SmoothQuant量化前对整数进行线性插值，保持精度，但量化后精度下降。 |

---

### 样本 3

**问题**: 你提到的“自研P2P中转服务器”和标准TURN服务器有什么区别？为什么选择自研？

| 角色 | 内容 |
|------|------|
| 📝 参考答案 | 自研中转服务完全去掉了STUN协商和权限机制，会话建立由信令服务控制，中继服务仅需根据信令指令建立一对一通道，监听端口接收客户端TCP连接或UDP包，并互相转发。由于不引入TURN协议，减少了编解码开销，延迟降低约5ms。同时可以根据业务需要灵活插入限流、监控等功能。缺点是缺乏标准化，无法被第三方WebRTC客户端直接使用。在我们的闭环生态中，这是最优选择。 |
| 🔴 Base 模型 | system
You are a helpful assistant.
user
你提到的“自研P2P中转服务器”和标准TURN服务器有什么区别？为什么选择自研？
assistant
<think>
嗯，用户问的是“自研P2P中转服务器”和“标准TURN服务器”的区别，以及为什么选择自研。首先，我需要理解这两个概念的具体定义和应用场景。

自研P2P中转服务器应该是指自己开发的，而不是依赖第三方的服务器。而标准TURN服务器通常指的是基于现有技术的，比如使用现有的网络协议或服务，可能由运营商或企业维护。用户可能想知道它们在功能、性能、成本、安全性等方面的不同。

接下来，用户可能没有明确说明他们的使用场景，比如是在企业环境中部署，还是个人项目。需要考虑不同情况下的差异。比如，自研服务器可能更灵活，可以定制化，但成本可能更高；而标准服务器可能更稳定，但可能缺乏定制化选项。

用户可能还关心为什么选择自研，比如成本效益、安全性、可扩展性等。需要解释这些因素，帮助用户理解为什么自研更合适。同时，可能需要提到自研服务器的局限性，比如需要更多开发资源，或者对现有技术的依赖，但这也是一个需要权衡的... |
| 🟢 LoRA 模型 | system
You are a helpful assistant.
user
你提到的“自研P2P中转服务器”和标准TURN服务器有什么区别？为什么选择自研？
assistant
<think>
### 面试官目的
考察对中转服务器架构的理解，以及自主化部署的必要性。

### 回答思路
说明标准TURN服务器的局限性（如带宽、稳定性、扩展性），自研服务器可灵活配置、自研协议、自研监控。

### 思考过程
标准TURN服务器通常基于现有协议栈，可能无法支持自定义协议，且维护成本高。自研中转服务器可开发自己的协议栈，实现自定义的QoS、自定义的中继路由，且可集成自研的监控和日志系统，满足高并发、高可用、自定义需求。
</think>

自研中转服务器可开发自己的TCP/IP协议栈，实现自定义的QoS、自定义的路由算法，支持自定义的中继端口和端口映射，且可集成自研的监控和日志系统，满足高并发、高可用、自定义需求。相比标准TURN服务器，自研服务器更灵活、可控。 |

---

### 样本 4

**问题**: 什么是大模型中的提示压缩（Prompt Compression）中的LLMLingua？如何利用小模型压缩提示？

| 角色 | 内容 |
|------|------|
| 📝 参考答案 | LLMLingua利用轻量语言模型计算长提示中各token的困惑度，保留高困惑度（信息量大）的token，丢弃冗余token，实现可控比例的提示压缩。压缩后的短提示送入大模型，在几乎不损失性能的情况下大幅节省推理成本。 |
| 🔴 Base 模型 | system
You are a helpful assistant.
user
什么是大模型中的提示压缩（Prompt Compression）中的LLMLingua？如何利用小模型压缩提示？
assistant
<think>
嗯，用户问的是大模型中的提示压缩（Prompt Compression）中的LLM中的具体含义，以及如何利用小模型压缩提示。首先，我需要确认用户的问题是否正确。提示压缩通常指的是将长文本的提示信息压缩到更小的模型中，以提高效率或减少计算资源消耗。LLM在这里可能指的是大型语言模型，也就是大模型，而用户可能混淆了LLM和LLM（可能是指其他缩写）。

接下来，我需要解释提示压缩的基本概念。提示压缩的核心思想是将长文本的提示信息压缩，使得模型在处理时不需要存储过多的详细信息。这通常通过将长提示拆分为更短的子提示，或者使用更高效的编码方式来实现。例如，使用Transformer的自注意力机制可以更有效地捕捉长距离依赖关系，而压缩后的提示信息则可以更紧凑地表示原始文本。

然后，用户可能想知道如何利用小模型进行提示压缩。这里需要分步骤说明。首先，确定原始提示的长度和... |
| 🟢 LoRA 模型 | system
You are a helpful assistant.
user
什么是大模型中的提示压缩（Prompt Compression）中的LLMLingua？如何利用小模型压缩提示？
assistant
<think>
### 面试官目的
提示压缩技术。
### 回答思路
LLMLingua将长提示拆分为多个小提示，每个小提示由一个轻量模型（如LLaMA-7B）生成，然后将这些小提示拼接，生成长提示。这减少了提示长度，同时保持了模型的性能。
### 思考过程
LLaMA-7B是轻量模型，其参数量远小于LLaMA-3B。LLMLingua将原始长提示拆分为多个小提示，每个小提示由LLaMA-7B生成。拼接后，生成的长提示在推理时由LLaMA-7B处理，性能几乎不变。这使得在提示长度受限时，仍能保持高精度。
</think>

LLMLingua通过将长提示拆分为多个小提示，每个小提示由轻量模型生成，然后拼接。这将提示长度压缩至原始的1/10，同时保持模型性能，是提示压缩中非常有效的技术。 |

---

### 样本 5

**问题**: 在行人重识别中，如何解决跨视角的“外观变异”问题？多尺度全局-局部特征融合网络是如何设计的？

| 角色 | 内容 |
|------|------|
| 📝 参考答案 | 行人重识别网络（如PCB）将特征图水平切分，每块做独立分类损失，使得模型学到视角变化下稳定的局部细节（如背包、鞋子），显著提升跨摄像头匹配精度。 |
| 🔴 Base 模型 | system
You are a helpful assistant.
user
在行人重识别中，如何解决跨视角的“外观变异”问题？多尺度全局-局部特征融合网络是如何设计的？
assistant
<think>
好的，用户问的是在行人重识别中如何解决跨视角的“外观变异”问题，以及多尺度全局-局部特征融合网络的设计。我需要先理解这两个问题的具体内容。

首先，跨视角的外观变异，可能是指在不同视角下，比如从不同角度拍摄的行人，他们的面部特征可能有变化，比如眼睛位置、嘴巴形状等。这时候需要解决这些变异带来的识别困难。常见的方法可能包括使用多视角的特征提取，或者引入对抗训练来增强模型鲁棒性。

然后是多尺度全局-局部特征融合网络的设计。这里可能需要考虑如何将不同尺度的特征结合起来，比如全局特征来自大尺度的特征图，局部特征来自小尺度的细节。可能需要使用卷积神经网络中的多尺度特征提取模块，比如使用不同分辨率的卷积核，或者使用注意力机制来整合不同尺度的信息。

我需要先回忆一下相关的研究，比如在行人重识别中，是否有使用多视角的特征提取方法，或者多尺度融合网络。比如，可能使用了Transformer结... |
| 🟢 LoRA 模型 | system
You are a helpful assistant.
user
在行人重识别中，如何解决跨视角的“外观变异”问题？多尺度全局-局部特征融合网络是如何设计的？
assistant
<think>
### 面试官目的
多模态特征融合。
### 回答思路
使用全局特征池和局部特征融合。
### 思考过程
行人特征在不同视角下分布不同。网络先提取全局特征池，再通过局部特征融合网络将局部特征与全局特征拼接，最后进行分类。
</think>

多尺度融合网络将全局特征池和局部特征融合拼接，然后通过一个全局-局部特征融合网络，将全局特征和局部特征进行拼接，再通过一个全局-局部特征融合网络，将融合后的特征送入分类器。 |

---


## 4. LLM-as-Judge 质量评估

> 裁判模型：gemma-4-31B-it-Q4_0
> 评估样本数：5
> 评分维度：准确性、相关性、完整性、整体质量（1-5 分）

### 综合均分对比

| 维度 | Base | LoRA | Δ |
|------|------|------|-----|
| accuracy | 2.0 | 0.0 | -2.00 |
| relevance | 2.75 | 0.0 | -2.75 |
| completeness | 1.0 | 0.0 | -1.00 |
| overall | 1.0 | 0.0 | -1.00 |


> **Δ (LoRA - Base)**: 正值为提升，负值为退化。

### 逐条详细评分

### Judge 样本 1

**问题**: 如何用CNN实现视频的实时美颜和滤镜？需要什么样的网络结构？

| 维度 | Base | LoRA |
|------|------|------|
| accuracy | 1 | None |
| relevance | 1 | None |
| completeness | 1 | None |
| overall | 1 | None |

> 🔴 **Base 点评**: 学生模型仅输出了思考过程（think block），完全没有给出最终回答，属于严重的生成失败。

> 🟢 **LoRA 点评**: 

---
### Judge 样本 2

**问题**: 解释一下SmoothQuant量化的核心思想，与AWQ的主要区别是什么？

| 维度 | Base | LoRA |
|------|------|------|
| accuracy | None | None |
| relevance | None | None |
| completeness | None | None |
| overall | None | None |

> 🔴 **Base 点评**: 

> 🟢 **LoRA 点评**: 

---
### Judge 样本 3

**问题**: 你提到的“自研P2P中转服务器”和标准TURN服务器有什么区别？为什么选择自研？

| 维度 | Base | LoRA |
|------|------|------|
| accuracy | 3 | None |
| relevance | 3 | None |
| completeness | 1 | None |
| overall | 1 | None |

> 🔴 **Base 点评**: 回答在输出过程中突然中断，内容不完整，且未能触及参考答案中关于协议简化和延迟降低的核心技术要点。

> 🟢 **LoRA 点评**: 

---
### Judge 样本 4

**问题**: 什么是大模型中的提示压缩（Prompt Compression）中的LLMLingua？如何利用小模型压缩提示？

| 维度 | Base | LoRA |
|------|------|------|
| accuracy | 1 | None |
| relevance | 2 | None |
| completeness | 1 | None |
| overall | 1 | None |

> 🔴 **Base 点评**: 模型完全没有识别出 LLMLingua 是一个具体的提示压缩框架，将其误认为在询问通用的大模型提示压缩，且回答内容在途中截断，未能提供任何有效信息。

> 🟢 **LoRA 点评**: 

---
### Judge 样本 5

**问题**: 在行人重识别中，如何解决跨视角的“外观变异”问题？多尺度全局-局部特征融合网络是如何设计的？

| 维度 | Base | LoRA |
|------|------|------|
| accuracy | 3 | None |
| relevance | 5 | None |
| completeness | 1 | None |
| overall | 1 | None |

> 🔴 **Base 点评**: 回答在正文部分严重截断，仅写了开头的一句话，未能回答任何实质性内容，完全不可用。

> 🟢 **LoRA 点评**: 

---


---

## 5. 结论与建议

| 指标 | 判断标准 | 是否合格 |
|------|---------|---------|
| PPL 下降 | LoRA PPL < Base PPL | 待填充 |
| ROUGE-L > 0.2 | 生成与参考答案有实质重叠 | 待填充 |
| 过拟合检测 | PPL 未接近 1.0 | 待填充 |
| LLM-Judge | 4 维度均分 > 3.0 | 待填充 |

> **下一步建议**:
> 1. 若 ROUGE-L 过低而 PPL 正常 → 模型学到了风格但未学到内容 → 增加 epoch 或降低 lr
> 2. 若 PPL 接近 1.0 → 严重过拟合 → 降低 epoch、增大 dropout、减少 LoRA rank
> 3. 若 PPL 不降 → 学习率过低或数据格式有误 → 检查 chat_template
> 4. 若 LLM-Judge 评分低 → 数据质量或蒸馏策略需改进，检查 teacher 回答与 student 能力匹配度

---

*报告由 eval.py 自动生成*
